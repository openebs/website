[build]
  base = "docs/"
  publish = "build/"
  command = "npm install && npm run build"

[build.environment]
  NODE_VERSION = "24"

[context.deploy-preview]
  base = "docs/"
  publish = "/"
  command = "npm run previewBuild"
  
  # adding redirect rule to change the path to /docs
  
  [[redirects]]
    from = "/"
    to = "/docs"
